base_deploy_dir: "/srv"
deploy_dir: "{{ base_deploy_dir }}/corona-game-{{ env }}"

registry_url: "eu.gcr.io/corona-game-300009"

frontend_image: "corona-frontend-{{ env }}"
frontend_container: "{{ frontend_image }}"
frontend_remote_image: "{{ registry_url }}/{{ frontend_image }}"
frontend_remote_image_latest: "{{ frontend_remote_image }}:latest"

#backend_image: "corona-backend-{{ env }}"
#backend_container: "{{ backend_image }}"
#backend_remote_image: "{{ registry_url }}/{{ backend_image }}"
#backend_remote_image_latest: "{{ backend_remote_image }}:latest"

#static_image: "corona-static-{{ env }}"
#static_container: "{{ static_image }}"
#static_remote_image: "{{ registry_url }}/{{ static_image }}"
#static_remote_image_latest: "{{ static_remote_image }}:latest"

#backend_deploy_container: "corona-deploy-{{ env }}"

nginx_file: "nginx.conf"
nginx_dir: "/srv/nginx"
nginx_container: "nginx-proxy"

local_tmp_dir: "../../tmp"
